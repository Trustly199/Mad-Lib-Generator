# Mad-Lib-Generator
This is a simple Mad Libs story generator built in Python.  
The program asks the user to enter several words (adjective, noun, verb, place, animal).
The user’s inputs are stored in variables.
The program uses an f-string to insert the user’s words into a story template.
The completed story is printed to the screen.
